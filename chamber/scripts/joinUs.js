const joinButton = document.getElementById("cta");

// Add a click event listener to the button
joinButton.addEventListener("click", function() {
  // Redirect to the join.html page
  window.location.href = "join.html";
});